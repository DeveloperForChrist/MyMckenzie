### Neon / Auth environment variables (example)
# Copy this to a local `.env` and fill with your real values. Never commit a file named `.env` to source control.

STACK_PROJECT_ID=your_neon_stack_project_id_here
STACK_PUBLISHABLE_CLIENT_KEY=your_publishable_client_key_here
STACK_SECRET_SERVER_KEY=your_secret_server_key_here

# Database connection (owner / admin connection string)
# Example format (keep sslmode=require for Neon):
DATABASE_URL=postgresql://<db_user>:<db_password>@<host>/<database>?sslmode=require

# psql command example (copy/paste, adjust if needed):
# psql 'postgresql://<db_user>:<db_password>@<host>/<database>?sslmode=require&channel_binding=require'
