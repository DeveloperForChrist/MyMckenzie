// This project now uses a server that talks to Neon (Postgres).
// If you need client-side Neon settings, keep secrets server-side instead.
// Example environment usage (server):
// DATABASE_URL=postgresql://<user>:<pass>@<host>/<db>?sslmode=require

export const NOTE = 'This file is a placeholder. Auth & DB operations are handled by server/';
